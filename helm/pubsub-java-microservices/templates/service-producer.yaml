{{- if .Values.producer.enabled }}
{{- if not .Values.producer.job.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: producer
  labels:
    {{- if .Values.labels }}
    {{- toYaml .Values.labels | nindent 4 }}
    {{- end }}
    app.kubernetes.io/component: producer
spec:
  type: ClusterIP
  ports:
  - name: http
    port: 8080
  selector:
    {{- if .Values.selectorLabels }}
    {{- toYaml .Values.selectorLabels | nindent 4 }}
    {{- end }}
    app.kubernetes.io/component: producer
{{- end }}
{{- end }}

